# API base URL — no trailing slash (see src/api/config.ts)
VITE_API_URL=http://localhost:3001

# Public Razorpay Key ID only (e.g. rzp_test_…). Optional if POST /api/orders/checkout always returns keyId.
# Never add RAZORPAY_KEY_SECRET here — that belongs only on the server.

VITE_RAZORPAY_KEY_ID=
